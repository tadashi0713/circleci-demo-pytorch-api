version: 2.1

orbs:
  python: circleci/python@2.0.3

jobs:
  test:
    executor: python/default
    steps:
      - checkout
      - python/install-packages:
          pkg-manager: pipenv
      - run: pipenv run pytest --junitxml=test-results/junit.xml
      - store_test_results:
          path: test-results  

  test_parallel:
    parallelism: 10 # Parallelism
    executor: python/default
    steps:
      - checkout
      - python/install-packages:
          pkg-manager: pipenv
      - run: | 
          TEST_FILES=$(circleci tests glob "tests/test_*.py" | circleci tests split --split-by=timings)
          pipenv run pytest TEST_FILES --junitxml=test-results/junit.xml
      - store_test_results:
          path: report.xml

workflows:
  sample:
    jobs:
      - test
      - test_parallel